# AI Transcriber

Простое веб-приложение для транскрибации аудио/видео файлов и их последующего анализа с помощью локально запущенной LLM.

![screenshot](https://user-images.githubusercontent.com/1257582/236862355-35561c69-3a79-4470-9b5a-4e01361b7b09.png)

## 🚀 Особенности

*   **Транскрибация:** Преобразование речи из аудио/видео в текст с помощью `faster-whisper`.
*   **AI-анализ:** Выделение главной мысли и ключевых тезисов из текста с помощью `Ollama` и модели `Llama 3.2`.
*   **Веб-интерфейс:** Простой и понятный интерфейс на базе `Streamlit`.
*   **Локальный запуск:** Все компоненты работают локально в Docker-контейнерах, не требуя доступа к внешним API.
*   **Кроссплатформенность:** Поддерживаются архитектуры ARM (Apple Silicon) и x86_64.

## 🛠️ Технологический стек

*   **Бэкенд и UI:** Python, Streamlit
*   **Транскрибация:** `faster-whisper` (модель `medium`)
*   **AI-модель:** `Ollama` (модель `llama3.2:3b`)
*   **Оркестрация:** Docker, Docker Compose

## 📋 Требования

Для запуска проекта на вашем компьютере должен быть установлен **Docker** и **Docker Compose**.

*   [Установить Docker Desktop](https://www.docker.com/products/docker-desktop/)

## ⚙️ Инструкция по запуску

### 1. Клонирование репозитория

```bash
git clone <URL репозитория>
cd <папка проекта>
```

### 2. Создание директории для данных

Приложение использует папку `data` для хранения загруженных файлов и кэша AI-моделей. Создайте ее в корне проекта:

```bash
mkdir data
```

### 3. Запуск проекта

Проект запускается с помощью одной команды. Выберите файл в зависимости от архитектуры вашего процессора:

*   **Для Apple Silicon (ARM):**
    ```bash
    docker-compose -f docker-compose-arm.yml up --build -d
    ```

**Что произойдет после запуска:**
1.  Docker начнет сборку образа для приложения `whisper`.
2.  Будет скачан образ `ollama/ollama`.
3.  При первом запуске `ollama` скачает модель `llama3.2:3b` (около 1.7 ГБ). Это может занять некоторое время.
4.  Контейнеры запустятся в фоновом режиме (`-d`).

### 4. Доступ к приложению

После успешного запуска всех контейнеров откройте браузер и перейдите по адресу:

**[http://localhost:8501](http://localhost:8501)**

## ▶️ Как пользоваться

1.  **Загрузите файл:** Нажмите на кнопку "Загрузите видео или аудио" и выберите поддерживаемый файл (`mov`, `mp4`, `mp3`, `wav`, `m4a`).
2.  **Начните транскрибацию:** Нажмите кнопку `🚀 1. Начать транскрибацию`. Дождитесь завершения процесса.
3.  **Отредактируйте текст (опционально):** Вы можете исправить ошибки в распознанном тексте прямо в текстовом поле.
4.  **Запустите анализ:** Выберите язык ответа AI ("Русский" или "Английский") и нажмите `🧠 2. Анализировать текст`.
5.  **Получите результат:** Итоги анализа появятся ниже. Вы можете скачать их в виде `.txt` файла.

## ⏹️ Остановка проекта

Чтобы остановить все запущенные контейнеры, выполните команду в терминале из корня проекта:

*   **Для Apple Silicon (ARM):**
    ```bash
    docker-compose -f docker-compose-arm.yml down
    ```


Эта команда остановит и удалит контейнеры, но сохранит все данные в папке `data` (включая скачанную модель).
